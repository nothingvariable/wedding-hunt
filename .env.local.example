# PostgreSQL connection string (required)
DATABASE_URL=postgresql://user:password@localhost:5432/wedding_hunt

# Directory where uploaded photos are stored (optional, defaults to /data/photos)
PHOTO_DIR=/data/photos

# Max upload size in bytes (optional, defaults to 10MB)
MAX_UPLOAD_SIZE=10485760
